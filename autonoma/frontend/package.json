{
  "name": "autonoma-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "React SPA for the Autonoma agent command center (optional; the backend also serves a zero-build dashboard).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
